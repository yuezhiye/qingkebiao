<?xml version="1.0" encoding="utf-8"?>
<!--
  轻课表 · 桌面小组件 · Android 清单

  ⚠️ receiver 的 android:name 必须写全限定名，否则系统找不到类 → 小组件加不上桌面。
     规则：uni_modules 下的 UTS 插件 → uts.sdk.modules.(插件ID转驼峰)
           qkb-widget → uts.sdk.modules.qkbWidget

  ⚠️ android:exported 必须为 true —— APPWIDGET_UPDATE 是**系统（launcher /
     AppWidgetService）发来的跨应用广播**，设 false 系统发不进来，组件无法工作。

  ⚠️ **package 属性必须写**，且必须与 Kotlin 文件的 package 完全一致（2026-09-17 实测踩坑）：
     aapt2 编译插件资源时报 `<manifest> must have a 'package' attribute`；
     若这里漏写，R 类就生成不出来 → Kotlin 侧报 `找不到名称"R"`。
     规则：uni_modules 下的 UTS 插件 → uts.sdk.modules.(插件ID转驼峰)
           qkb-widget → uts.sdk.modules.qkbWidget
     **三处必须完全一致**：本文件 package / 所有 .kt 的 package / index.uts 的 import 包名。

  ⚠️ 本文件任何改动都需要**重新制作自定义基座 / 云打包**才生效，标准基座不加载。

  权限：无。小组件只读本地 SharedPreferences，不联网、不申请任何权限。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="uts.sdk.modules.qkbWidget">

  <application>

    <!-- ── 桌面小组件（4×2：今天课程，静态 3 槽位）── -->
    <receiver
      android:name="uts.sdk.modules.qkbWidget.QkbWidgetProvider"
      android:exported="true"
      android:label="@string/qkb_widget_name_4x2">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/qkb_widget_4x2_info" />
    </receiver>

    <!--
      ── ⛔ 已废弃：「横向课程列表」的数据源 Service（保留未删，待清理）──

      【当前状态】**没有任何代码引用它**。小组件现在是「静态 3 槽位」纯布局，
        不走 ListView，因此不需要 RemoteViewsService。
        （布局见 res/layout/qkb_widget_static.xml）

      【为什么当初加它】
        曾想用 ListView + setRemoteAdapter 做「左右滑动逐节查看」。
        而 ListView 的内容不能在 provider 里直接塞，必须走官方链路：
          setRemoteAdapter(intent) → 系统绑定本 Service → Factory.getViewAt() 逐项渲染。

      【为什么废弃了】2026-09-18 真机（vivo OriginOS）实测证伪：
        系统**从不绑定**这个 Service（`dumpsys activity services | grep QkbWidget` 恒为空），
        桌面只显示 launcher 默认占位（白卡片 + 转圈），点击无反应。
        根因：RemoteViews 里**能滚动的控件只有 ListView / AdapterViewFlipper**，
        且二者都必须配 RemoteViewsService —— 这条链路一旦不被系统绑定，就没有替代方案。
        → 横向滚动在桌面小组件里**做不了**，遂整体改为静态槽位。

      【保留原因 + 清理条件】
        保留是为了「万一将来要复现排查」。删除前需确认：
          ① QkbWidgetService.kt 与 QkbWidgetFactory 一并删除（否则类还在、Service 未注册，无害但冗余）
          ② res/layout/qkb_widget_4x2.xml + qkb_widget_4x2_item.xml 一并删除（均无引用）
        上述文件在 0.2.0 中**保持原样未动**，避免发布前引入变量。

      ⚠️ android:permission="android.permission.BIND_REMOTEVIEWS" ——
         若将来恢复此链路，该权限必须写，否则系统拒绝绑定；漏写则列表恒为空且不报错。
      ⚠️ android:exported="false" —— 只允许系统通过权限绑定，不对外开放。
    -->
    <service
      android:name="uts.sdk.modules.qkbWidget.QkbWidgetService"
      android:exported="false"
      android:permission="android.permission.BIND_REMOTEVIEWS" />

  </application>
</manifest>
