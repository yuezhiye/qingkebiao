# 更新日志

本项目的所有重要变更都记录在此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> ⚠️ **本项目尚未发布 1.0**。所有 `0.x` 版本均视为测试版，
> 界面布局与数据格式仍可能在没有兼容层的情况下调整。
> 仓库的 [Releases](https://github.com/yuezhiye/qingkebiao/releases) 页面均标记为 Pre-release。

---

## [0.2.0] - 2026-09-23

**主题：桌面小组件** —— 不用打开 App，在桌面就能看到今天的课。

### 新增

#### 桌面小组件（4×2）

新增原生桌面组件「轻课表 · 今日课程」，尺寸 4×2，直接显示当天课程：

- **固定头部**：`今天 · 周二 · 3 节` + 右侧周次 `第 4 周`
- **课程卡**：最多同时显示 3 节，每张卡含
  - 时间段（如 `08:00 - 09:40`）+ 节次（如 `第 1-2 节`）
  - 课程名
  - 上课地点
  - **卡片底色**按课程名自动配色（同课同色，与 App 内一致）
- **空态**：当天没课时显示「今天没有课」+「好好休息」，不留空白框
- **溢出提示**：超过 3 节时底部显示「还有 N 节 · 点开查看」
- **点击跳转**：点击组件任意位置直接打开 App

#### 实时联动

**在 App 里改完课表，桌面组件立刻刷新**，无需等待系统的周期刷新。

具体实现：App 的课程数据读取统一收口在 `refresh()`，该函数被调用时
（进入页面 / 返回页面 / 导入课表 / 编辑课程后）自动把最新数据推给桌面组件。

#### 其他

- 新增解析器自测断言 60 项（107 → **167 项**），覆盖新增的数据组装与裁剪逻辑

### 修复

- **修复 `plus is not defined` 隐患**：`pages/settings/settings.vue` 中导出 JSON 的两个函数
  （`deliverJsonFile` / `saveJsonToDownloads`）依赖 App 端专属 API（`plus.android.*`），
  但此前未加条件编译包裹。当前只发 Android，故未实际触发；若将来编译到 H5 / 小程序端会直接报错。
  现整体包在 `APP-PLUS` 分支内，非 App 端该函数退化为返回 `false`，调用方走降级分支。

- **修复桌面组件色块颜色与 App 不一致**（真机验收发现）：
  同一门课在 App 与桌面显示的色块颜色不同，且两门课的颜色**正好互换**。
  根因是**双重哈希** —— App 侧 `utils/widget.js` 传下去的 `colorSeed` **已经是色号**
  （0~5 的整数，因为 RemoteViews 只能 `setBackgroundResource(预置资源id)`），
  但原生侧 `WidgetData.kt` 把它当成**种子字符串**又哈希了一次
  （如色号 `5` → `hashString("5") % 6 = 2`），导致颜色整体错位。
  现改为直接把 `colorSeed` 当索引用并在两侧写明契约；
  字段缺失或非法时回退按课程名哈希（不越界、不崩溃）。

### 变更

- 版本号 `0.1.0 / 100` → **`0.2.0 / 101`**
- 组件描述文案由「今天的课程，左右滑动逐节查看」改为「在桌面一眼看全今天的课程安排」
  —— 原描述承诺了左右滑动，而该功能经真机验证**无法实现**（原因见下）
- 清理了未实现的 2×2 组件文案资源（`qkb_widget_name_2x2` / `qkb_widget_desc_2x2`）

### 已知限制

- **必须使用自定义基座运行**：组件是原生 UTS 插件，标准基座（HBuilder 基座）不加载它
- **不支持左右滑动**：Android 桌面组件里能滚动的控件只有 `ListView` / `AdapterViewFlipper`，
  且二者都必须搭配 `RemoteViewsService`。本项目在真机（vivo OriginOS）上实测该链路
  **从未被系统绑定**，桌面只显示默认占位（白卡片 + 转圈）且点击无反应，
  故整体改为静态槽位方案。横向滚动在桌面组件里**做不了**。
- **自动刷新最快 30 分钟**：`updatePeriodMillis` 的 Android 硬下限，
  故组件**不显示分钟级倒计时**（会明显滞后）。实时性靠 App 打开时主动推送。
- **仅 `arm64-v8a`**：32 位老机型装不上

---

## [0.1.0] - 2026-09-14

首个公开测试版（Releases 标记为 `v0.1.0-beta.1`）。

### 新增

- **本地离线运行**：无账号、无网络请求、零权限、零 npm 依赖
- **课表网格**：7 天 × 10 节，每 2 节合成一行；同格多门课时上下分半显示，并用角标标注各自周次；当前时段加主色描边
- **稳定取色**：同一门课颜色恒定
- **今日课程卡**：当天全部课程 + 距下节课实时倒计时
- **三通道导入**
  - 文件导入 —— 直接读取手机里的课表 `.txt`（零权限），**仅适配景德镇艺术职业大学**
  - AI 法 —— 把课表截图 / 文字交给任意 AI，按内置提示词输出 JSON 后粘贴，**各校通用**
  - 手写法 —— 按标准文本格式逐行手动填写，**各校通用**
- **数据导出**：标准文本 / JSON 文件（JSON 写入系统「下载」目录）
- **首启空白**：不预置任何示例数据；清空课程后重启也不会长回来
- 解析器自测 107 项断言（`node self-test.mjs`）

### 已知限制

- 未做机型兼容性测试
- 「本地表格解析」只支持景德镇艺术职业大学，其他学校请改用 **AI 法**
- 手机剪贴板粘贴大段文本会静默截断，课表文件请走「文件导入」

---

[0.2.0]: https://github.com/yuezhiye/qingkebiao/compare/v0.1.0-beta.1...v0.2.0
[0.1.0]: https://github.com/yuezhiye/qingkebiao/releases/tag/v0.1.0-beta.1
